#include "patterns.h"

int main() {
  patterns::map();
  patterns::stencil();
  patterns::reduce();
  patterns::scan();
  patterns::pack();
  patterns::gather();
  patterns::workpile();

  return 0;
}
